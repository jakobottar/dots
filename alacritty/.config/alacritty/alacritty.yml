# Configuration for Alacritty, the GPU enhanced terminal emulator.

# Import additional configuration files
import:
 - ~/.config/alacritty/dracula.yml

# change $TERM variable so stuff works on remote computers
env:
  TERM: xterm-256color

# change font
font: 
  normal:
    family: Fira Code
  size: 10.5
